# B 端 PM Skill 业务缺失清单

> 基于对现有 skill 的全量审查，从 B 端产品经理真实日常工作流出发，整理尚未覆盖的业务场景、业务模式和交付能力。
>
> 分类维度：场景覆盖 / 业务模式 / 交付能力 / 流程能力

---

## 一、场景覆盖缺失

当前 skill 只覆盖"从零新建功能"这一种场景。B 端 PM 日常工作中，新功能只占 30-40%。

### 1.1 需求变更（日常占比 20-25%）🔴 P0

**现状：** 无变更入口，所有需求都走完整 9 步流程。

**典型场景：**
- "已上线的报销流程，加一个总经理额外审批节点"
- "合同模块字段名从'甲方'改成'客户名称'"
- "审批流从三级改成两级，去掉法务审批"
- "新增一个'紧急'标记，优先展示在列表顶部"

**缺失能力：**
- [ ] 识别"变更需求"关键词（在已有功能上、修改、调整、去掉、新增XX节点）
- [ ] 读取已有 project-state.json，定位要变更的实体（哪个 page/module/field/action/transition）
- [ ] 评估变更影响范围（改了审批流 → 哪些 PRD section、哪些标注、哪些测试用例受影响）
- [ ] 生成变更说明（diff 格式：变更前 → 变更后）而非完整 PRD
- [ ] 变更后的回归检查（只检查波及部分，不全量重新检查）
- [ ] 变更记录归档（project-state.json 中记录每次变更的时间、内容、影响范围）

**建议实现路径：** SKILL.md 增加"变更模式"入口——当检测到已有项目目录且用户描述含变更语义时，走变更流程而非完整流程。

---

### 1.2 配置型需求（日常占比 10-15%）🔴 P0

**现状：** 枚举值变更、阈值调整等简单需求也走完整 9 步流程，过重。

**典型场景：**
- "在费用类型下拉框新增一个'团建费'"
- "审批金额阈值从 5 万改成 10 万"
- "给财务角色增加'导出月度报表'权限"
- "客户等级新增一个'战略客户'选项"
- "列表页默认排序从创建时间改为更新时间"

**缺失能力：**
- [ ] 识别配置型需求关键词（新增选项、修改阈值、调整权限、变更默认值）
- [ ] 走精简流程：配置变更说明 → 影响分析 → 回归测试点（3 步，不需要原型和完整 PRD）
- [ ] 自动生成配置清单（字典配置 SQL、权限配置项、前端枚举值）
- [ ] 自动列出受影响的页面和功能（改了费用类型枚举 → 哪些页面的下拉框需要更新）

**建议实现路径：** 在 conversation-workflow.md 增加"配置变更"节点，识别后走精简路径。

---

### 1.3 Bug 修复需求（日常占比 10-15%）🟡 P1

**现状：** 无 Bug 修复需求的处理流程。

**典型场景：**
- "报销单提交后状态没有更新"
- "审批人在详情页看不到驳回按钮"
- "导出的 Excel 金额列格式不对"
- "列表页筛选条件重置后数据没刷新"

**缺失能力：**
- [ ] 识别 Bug 修复需求关键词（XX不对、XX没生效、XX报错、XX显示异常）
- [ ] 生成 Bug 修复需求卡（问题描述、复现步骤、期望行为、影响范围）
- [ ] 评估修复影响（是否涉及状态机、权限、数据一致性）
- [ ] 生成回归测试点（修完后需要验证什么）

---

### 1.4 数据迁移需求（低频但复杂）🟡 P1

**现状：** 无数据迁移建模。

**典型场景：**
- "把老系统的供应商数据迁移到新系统"
- "历史合同数据需要补充新增的'合同类型'字段"
- "Excel 中的客户数据批量导入系统"

**缺失能力：**
- [ ] 识别数据迁移需求关键词（迁移、导入、历史数据、批量导入、数据清洗）
- [ ] 生成数据迁移方案（数据来源 → 字段映射 → 清洗规则 → 校验规则 → 回滚方案）
- [ ] 字段映射表（老系统字段 → 新系统字段 → 转换规则）
- [ ] 迁移验证方案（迁移后数据条数、关键字段校验、抽样比对）

---

### 1.5 系统对接需求（低频但复杂）🟡 P1

**现状：** L 级 PRD 有 interface_integration section，但需求澄清阶段不会主动识别。

**典型场景：**
- "报销审批通过后自动推送到财务系统生成凭证"
- "采购订单需要同步到 ERP 系统"
- "审批结果需要推送到企业微信通知"
- "需要从 HR 系统同步组织架构和员工信息"

**缺失能力：**
- [ ] 识别系统对接需求关键词（对接、同步、推送、拉取、集成、XX系统）
- [ ] 需求澄清阶段主动追问：对接哪个系统、数据流向、同步频率、异常处理
- [ ] 生成接口对接方案（接口清单、数据格式、调用方式、重试策略、降级方案）
- [ ] 生成字段映射表（本系统字段 ↔ 对方系统字段）
- [ ] 异常场景覆盖（对方系统不可用、数据格式不匹配、推送失败重试）

---

## 二、业务模式缺失

B 端产品的高频业务模式，当前 skill 未建模或建模不足。

### 2.1 主数据管理 🟡 P1

**现状：** skill 没有"主数据"概念，不会主动识别和建模主数据。

**B 端现实：** 几乎所有 B 端系统的业务单据都依赖主数据（客户、供应商、产品、组织架构、员工、仓库、银行账户等）。

**缺失能力：**
- [ ] 识别主数据需求（当需求涉及"客户""供应商""产品"等实体时，追问主数据来源和维护方式）
- [ ] 主数据建模模板：
  - 数据来源：手动录入 / Excel 导入 / 外部系统同步
  - 维护角色：谁创建、谁修改、谁停用
  - 引用关系：业务单据引用主数据后，主数据修改了怎么办（版本快照 vs 实时引用）
  - 生命周期：启用 / 停用 / 归档
- [ ] 主数据与业务数据的关联规则

---

### 2.2 数据权限层级细化 🟡 P1

**现状：** 数据权限只有 `self / department / all` 三级。

**B 端现实：** 真实数据权限远比三级复杂。

**缺失能力：**
- [ ] 支持更细粒度的数据权限模型：

| 权限范围 | 含义 | 示例 |
|----------|------|------|
| `self` | 只看自己创建的 | 经办人看自己的报销单 |
| `department` | 看本部门 | 部门经理看本部门报销单 |
| `department_children` | 看本部门及下级部门 | 事业部总监看下属所有部门 |
| `project` | 看指定项目 | 项目经理看本项目合同 |
| `region` | 看指定区域 | 区域经理看本区域客户 |
| `all` | 看全部 | 管理员看所有数据 |

- [ ] 需求澄清阶段追问数据权限范围
- [ ] 原型中体现不同角色看到不同数据范围

---

### 2.3 批量操作 🟡 P1

**现状：** 原型和 PRD 模板中没有批量操作建模。

**B 端现实：** 列表页几乎都有批量操作需求。

**缺失能力：**
- [ ] 原型生成时自动在列表页增加批量操作区域（全选/反选 + 批量操作按钮）
- [ ] PRD 模板增加批量操作说明 section：
  - 支持哪些批量操作（批量审批、批量导出、批量删除、批量分配）
  - 批量操作的前置条件（所有选中项必须处于同一状态？）
  - 部分成功处理（10 条批量审批，7 条成功 3 条失败 → 怎么展示结果）
  - 批量操作的数量限制（单次最多 100 条？500 条？）
  - 批量操作的权限（是否需要更高权限）

---

### 2.4 编码规则 🟢 P2

**现状：** 无编码规则建模。

**B 端现实：** 几乎所有 B 端单据都有编号（`REQ-2026-0001`、`PO-202605-001`）。

**缺失能力：**
- [ ] 需求澄清阶段追问编码规则（编号格式、前缀、生成时机）
- [ ] PRD 增加编码规则说明：
  - 编号格式：前缀 + 年月 + 流水号
  - 生成时机：创建时 / 提交时
  - 是否可修改
  - 是否连续（删除后是否跳号）
  - 跨年重置规则

---

### 2.5 操作日志与审计 🟡 P1

**现状：** 原型只有 `module_timeline`（状态记录），没有操作日志。PRD 无操作日志 section。

**B 端现实：** 操作日志是 B 端标配，也是合规要求。

**缺失能力：**
- [ ] 原型自动生成操作日志模块（详情页底部）
- [ ] PRD 增加操作日志说明 section：
  - 记录哪些操作（创建、修改、删除、审批、导出）
  - 记录哪些字段变更（字段名、旧值、新值）
  - 操作人、操作时间、IP 地址
  - 日志保留周期
- [ ] 审计追溯需求（谁在什么时间做了什么、改了什么）

---

### 2.6 报表与数据统计 🟢 P2

**现状：** 无报表/统计建模。

**B 端现实：** B 端 PM 经常需要定义统计看板和报表需求。

**缺失能力：**
- [ ] 识别报表需求关键词（统计、看板、报表、汇总、分析、趋势）
- [ ] 报表需求建模模板：
  - 统计维度（按部门 / 按类型 / 按时间 / 按状态）
  - 统计指标（总金额 / 笔数 / 平均值 / 同比 / 环比）
  - 筛选条件
  - 数据口径（哪些状态的单据计入、是否包含已撤回的）
  - 数据更新频率（实时 / T+1 / 每周一）
  - 图表类型（表格 / 柱状图 / 饼图 / 趋势图）
- [ ] 数据口径一致性检查（报表的数字和列表页的数字是否对得上）

---

### 2.7 导入导出增强 🟢 P2

**现状：** 需求中提到"导出"时会生成导出功能，但没有详细的导入导出规则建模。

**B 端现实：** 导入导出是 B 端高频功能，规则复杂。

**缺失能力：**
- [ ] 导出规则建模：
  - 导出格式（Excel / CSV / PDF）
  - 导出字段范围（全部字段 / 当前筛选结果 / 选中行）
  - 导出数据量限制（单次最多多少条）
  - 导出权限（谁能导出）
  - 异步导出（大数据量时后台生成，完成后通知下载）
- [ ] 导入规则建模：
  - 导入模板下载
  - 文件格式和大小限制
  - 字段校验规则
  - 错误处理（部分成功怎么办、错误行如何标记）
  - 重复数据处理（跳过 / 覆盖 / 报错）

---

## 三、交付能力缺失

### 3.1 业务确认单 🟡 P1

**现状：** 交付物面向研发和测试，缺少面向业务方的确认材料。

**缺失能力：**
- [ ] 自动生成业务确认单（精简版，非技术语言）：
  - 需求概述（一段话说清楚做什么）
  - 功能清单（用业务语言，不用技术术语）
  - 流程图（业务方能看懂的简化版）
  - 确认签字区域
- [ ] 适用于 S/M 级需求，给业务方签字确认

---

### 3.2 需求摘要（一页纸） 🟢 P2

**现状：** 无面向管理层的精简摘要。

**缺失能力：**
- [ ] 自动生成一页纸需求摘要：
  - 背景和目标（为什么做）
  - 方案概述（怎么做，3-5 句话）
  - 影响范围（涉及哪些模块、多少页面）
  - 工期预估（基于 S/M/L 复杂度）
  - 风险和依赖
- [ ] 适用于给领导汇报、立项评审

---

### 3.3 上线交付包 🟡 P1

**现状：** L 级 PRD 有 deployment section，但内容薄，缺少结构化的上线清单。

**缺失能力：**
- [ ] 一致性检查通过后，自动生成上线交付包：
  - **权限配置清单**：从 permission 实体反推，列出需要配置的角色、菜单、按钮权限
  - **字典配置清单**：从 field 的枚举值反推，列出需要新增的字典项
  - **上线检查 checklist**：功能验收、权限配置、数据迁移、灰度方案、回滚方案
  - **培训材料大纲**：哪些角色需要培训、重点讲什么功能

---

### 3.4 操作手册生成 🟢 P2

**现状：** 无面向终端用户的操作手册。

**缺失能力：**
- [ ] 基于原型和 PRD 自动生成操作手册大纲：
  - 按角色组织（员工操作手册 / 审批人操作手册 / 管理员操作手册）
  - 按场景组织（如何提交报销 / 如何审批报销 / 如何导出报表）
  - 每个操作包含：入口路径 → 操作步骤 → 注意事项 → 常见问题

---

## 四、流程能力缺失

### 4.1 跳步/精简模式 🟡 P1

**现状：** Anti-Pattern 明确禁止跳过任何步骤。

**B 端现实：** 有些场景确实不需要全流程。

**缺失能力：**
- [ ] 支持用户显式指定只做部分流程：
  - "我已经有 PRD 了，只需要做一致性检查"
  - "我只需要画原型，不需要 PRD"
  - "帮我只生成测试用例"
- [ ] 跳步时的前置条件检查（做一致性检查 → 必须有 PRD + 原型 + 标注）
- [ ] 跳步时不强制走其他节点

---

### 4.2 多项目关联 🟢 P2

**现状：** 每个项目独立运作，不感知其他项目。

**B 端现实：** B 端系统通常是多个模块联动的。

**缺失能力：**
- [ ] 识别跨模块需求（"报销系统需要关联合同系统的合同编号"）
- [ ] 跨项目实体引用（A 项目的 page 引用 B 项目的 business_object）
- [ ] 跨项目一致性检查（A 的字段名和 B 的字段名是否一致）

---

### 4.3 需求来源多样化 🟢 P2

**现状：** 需求来源只有"用户对话"和"参考资料文件"。

**B 端现实：** PM 的需求来源还包括：
- Jira/Linear/飞书 任务卡片
- 用户反馈汇总表
- 竞品分析文档
- 行业标准/合规要求

**缺失能力：**
- [ ] 支持从结构化任务卡片提取需求（解析 Jira/飞书导出的 JSON 或 CSV）
- [ ] 支持从竞品截图提取功能差异点
- [ ] 支持从合规文档提取约束条件

---

### 4.4 优先级排序与取舍 🟢 P2

**现状：** 功能清单列出所有 features，无优先级排序建议。

**缺失能力：**
- [ ] 在功能清单阶段生成优先级矩阵：
  - 横轴：实现难度（简单 / 中等 / 复杂）
  - 纵轴：业务影响（高 / 中 / 低）
  - 标注依赖关系（做 A 之前必须先做 B）
- [ ] 给出 MVP 建议（哪些是第一期必做，哪些可以第二期）
- [ ] 标注"可能被砍"的功能（复杂度高但影响低）

---

## 五、B 端领域知识缺失

### 5.1 常见 B 端页面模式库 🟡 P1

**现状：** 原型生成基于通用模板（列表页 / 详情页 / 创建页 / 审批页）。

**缺失的高频页面模式：**

| 页面模式 | 说明 | 当前是否覆盖 |
|----------|------|-------------|
| 列表页 + 筛选 + 分页 | 基础列表 | ✅ |
| 详情页 + 操作记录 | 基础详情 | ✅ |
| 新建/编辑表单页 | 基础表单 | ✅ |
| 审批处理页 | 审批流 | ✅ |
| 看板视图（Kanban） | 拖拽式状态管理 | ❌ |
| 树形结构页 | 组织架构、分类管理 | ❌ |
| 多 Tab 详情页 | 复杂对象详情（基本信息/关联数据/操作记录） | ❌ |
| 级联选择页 | 省市区、多级分类 | ❌ |
| 步骤表单（Stepper） | 分步填写复杂表单 | ❌ |
| 数据对比页 | 两个版本对比、变更前后对比 | ❌ |
| 仪表盘/看板 | 数据统计展示 | ❌ |
| 配置页面 | 系统设置、规则配置 | ❌ |

---

### 5.2 常见 B 端交互模式库 🟢 P2

**缺失的高频交互模式：**

| 交互模式 | 说明 |
|----------|------|
| 行内编辑 | 列表中直接编辑字段，不需要打开详情 |
| 快捷操作 | 列表行 hover 出现操作按钮 |
| 二次确认 | 删除、驳回等危险操作弹确认框 |
| 级联选择 | 选了部门自动过滤人员 |
| 条件显隐 | 选了"其他"才显示输入框 |
| 联动校验 | 结束日期不能早于开始日期 |
| 搜索防抖 | 输入时延迟搜索，避免频繁请求 |
| 空状态引导 | 列表为空时显示"新建第一个XX"按钮 |
| 加载骨架屏 | 数据加载中显示占位符 |
| 操作反馈 | 提交成功/失败的 toast 提示 |
| 撤销操作 | 删除后支持撤回（5秒内） |
| 快捷键 | Ctrl+S 保存、Esc 关闭弹窗 |

---

### 5.3 B 端异常场景标准库 🟡 P1

**现状：** 质量检查要求覆盖异常场景，但没有标准异常场景库。

**B 端标准异常场景清单：**

| 异常类型 | 场景 | 处理方式 |
|----------|------|----------|
| 数据不存在 | 访问已删除的单据 | 404 页面 + 返回列表 |
| 无权限 | 普通用户访问管理页面 | 403 页面 + 提示联系管理员 |
| 并发冲突 | 两人同时编辑同一条数据 | 提示"数据已被XX修改" + 刷新 |
| 状态冲突 | 已审批的单据再次审批 | 提示"该单据已处理" |
| 网络异常 | 提交时网络断开 | 保留表单数据 + 重试按钮 |
| 接口超时 | 导出大数据量 | 异步导出 + 完成后通知 |
| 必填字段为空 | 漏填提交 | 字段标红 + 错误提示 |
| 格式错误 | 金额填了文字 | 实时校验 + 错误提示 |
| 重复提交 | 快速双击提交按钮 | 按钮防抖 + loading 状态 |
| 数据过期 | 列表页长时间未刷新 | 操作时提示数据可能过期 |
| 批量操作部分失败 | 10 条批量审批 7 成功 3 失败 | 结果汇总 + 失败原因列表 |
| 导入数据异常 | Excel 中有格式错误行 | 错误行标红 + 错误原因 + 下载错误报告 |

---

## 六、优先级汇总

### P0 — 立即补充（影响用户感知最明显）

| 编号 | 缺失项 | 理由 |
|------|--------|------|
| 1.1 | 需求变更管理 | 占 PM 日常 20-25%，当前完全不支持 |
| 1.2 | 配置型需求精简流程 | 占 PM 日常 10-15%，走完整流程太重 |

### P1 — 尽快补充（提升专业度）

| 编号 | 缺失项 | 理由 |
|------|--------|------|
| 1.3 | Bug 修复需求 | 占 PM 日常 10-15% |
| 1.4 | 数据迁移需求 | 低频但高风险 |
| 1.5 | 系统对接需求 | 低频但必须覆盖 |
| 2.1 | 主数据管理 | B 端地基，不建模则后续功能都缺基础 |
| 2.2 | 数据权限层级细化 | self/department/all 三级不够用 |
| 2.3 | 批量操作 | B 端列表页标配 |
| 2.5 | 操作日志与审计 | B 端合规标配 |
| 3.1 | 业务确认单 | 面向业务方的交付物 |
| 3.3 | 上线交付包 | 每个功能上线都需要 |
| 4.1 | 跳步/精简模式 | 用户显式要求时应支持 |
| 5.1 | 常见 B 端页面模式库 | 看板、树形、多Tab、步骤表单等 |
| 5.3 | B 端异常场景标准库 | 异常覆盖的标准化 |

### P2 — 后续迭代（锦上添花）

| 编号 | 缺失项 | 理由 |
|------|--------|------|
| 2.4 | 编码规则 | 高频但可以手动补充 |
| 2.6 | 报表与数据统计 | 不是所有项目都需要 |
| 2.7 | 导入导出增强 | 当前基础版够用，增强可后续做 |
| 3.2 | 需求摘要（一页纸） | 面向管理层，使用频率低 |
| 3.4 | 操作手册生成 | 可以独立于主流程 |
| 4.2 | 多项目关联 | 复杂度高，收益不确定 |
| 4.3 | 需求来源多样化 | 当前对话+文件已覆盖 80% 场景 |
| 4.4 | 优先级排序与取舍 | 有价值但不是核心能力 |
| 5.2 | 常见 B 端交互模式库 | 丰富原型细节，非核心 |

---

## 七、建议实施路线图

### 第一阶段：补场景（2-3 周）

1. 增加"变更模式"入口（1.1）
2. 增加"配置变更"精简流程（1.2）
3. 增加"跳步模式"支持（4.1）

### 第二阶段：补模式（2-3 周）

4. 主数据建模模板（2.1）
5. 数据权限层级细化（2.2）
6. 批量操作建模（2.3）
7. 操作日志建模（2.5）
8. B 端异常场景标准库（5.3）

### 第三阶段：补交付（1-2 周）

9. 业务确认单生成（3.1）
10. 上线交付包自动生成（3.3）

### 第四阶段：补深度（持续迭代）

11. 系统对接需求（1.5）
12. 报表建模（2.6）
13. 页面模式库扩展（5.1）
14. 其余 P2 项
