# 开发者指南：改代码 + 提交全流程

> 写给小白的保姆级教程，看完就能独立修改代码并提交到 GitHub。

---

## 一、先搞懂：文件在哪里？

你现在有 **3 个地方** 存放着这个项目的代码，但只有 **1 个地方** 是你应该改的：

| 位置                  | 路径                                                                         | 用途                     | 要不要改？ |
| ------------------- | -------------------------------------------------------------------------- | ---------------------- | ----- |
| **Git 仓库（主目录）**     | `C:\Users\Administrator\b2b-pm-html-prototype-skill\codex 版本的skill-fused\` | **在这里改代码**             | ✅ 改这里 |
| Skill 加载点（Junction） | `C:\Users\Administrator\.claude\skills\pm-html-pdt-fused\`                 | Claude Code 加载 skill 用 | ❌ 不要改 |
| OneDrive 备份         | `C:\Users\Administrator\OneDrive\文档\个人开发\...\skill-fused\`                 | 旧的备份，已过时               | ❌ 不要改 |

### 为什么只有一个地方要改？

```
你改代码 ──→ Git 仓库（主目录）
                ↑
                │ junction（自动同步，零延迟）
                │
Claude Code ──→ Skill 加载点
```

`Skill 加载点` 是一个 **junction**（你可以理解为"快捷方式"），它指向 `Git 仓库`。所以你在 Git 仓库里改了文件，Claude Code 加载 skill 时自动就能读到最新代码。**改一处，两处都生效。**

---

## 二、改代码前的准备

### 步骤 1：打开 Git 仓库目录

在 Trae IDE 中，**打开文件夹**：

```
C:\Users\Administrator\b2b-pm-html-prototype-skill
```

> ⚠️ 注意：打开的是 `b2b-pm-html-prototype-skill` 这个文件夹，不是里面的 `codex 版本的skill-fused`。
> 打开后，在左侧文件树里就能看到 `codex 版本的skill-fused` 目录。

### 步骤 2：确认你在正确的分支

打开终端（Terminal），输入：

```bash
cd C:\Users\Administrator\b2b-pm-html-prototype-skill
git branch
```

你应该看到类似：

```
* main
```

带 `*` 号的就是你当前所在的分支。**日常开发就在 `main` 分支上改就行。**

### 步骤 3：确认没有未提交的改动

```bash
git status
```

如果你看到一堆红色的文件（未提交的修改），说明之前有改动还没提交。你可以先提交它们（见第五节），或者先不管，直接改你要改的。

---

## 三、改代码（实际操作）

### 你应该改哪些文件？

所有代码都在 `codex 版本的skill-fused/` 目录下，主要关注这些：

```
codex 版本的skill-fused/
├── src/
│   ├── agents/          ← AI Agent 的逻辑
│   ├── cli/             ← 命令行入口（cli.ts 是主入口）
│   ├── generators/      ← 各种生成器
│   ├── llm/             ← LLM 调用相关
│   ├── reviews/         ← 多角色审查系统（新加的）
│   ├── state/           ← 状态管理
│   ├── types/           ← 类型定义
│   └── workflow/        ← 工作流（generation-pipeline.ts 是核心）
├── prompts/             ← AI 的提示词（.md 文件）
├── tests/               ← 测试文件
├── SKILL.md             ← Skill 描述文件
├── README.md            ← 项目说明
└── 使用手册.md           ← 用户使用手册
```

### 改完后怎么确认改对了？

**两步验证，缺一不可：**

#### 第 1 步：类型检查（有没有语法错误）

```bash
cd "C:\Users\Administrator\b2b-pm-html-prototype-skill\codex 版本的skill-fused"
npx tsc --noEmit
```

- 如果输出没有任何内容 = ✅ 通过
- 如果输出红色的 `error TSxxxx` = ❌ 有语法错误，按提示修复

#### 第 2 步：跑测试（功能有没有被改坏）

```bash
npx vitest run
```

你应该看到类似：

```
Tests  86 passed
```

- 全部 `passed` = ✅ 通过
- 有 `failed` = ❌ 有问题，看输出的错误信息修复

> 💡 **小技巧**：如果你只改了某个模块，可以只跑相关测试：
> 
> ```bash
> npx vitest run tests/reviews/     # 只跑审查系统的测试
> npx vitest run tests/generators/  # 只跑生成器的测试
> ```

---

## 四、提交代码到 Git

确认 typecheck 和测试都通过后，就可以提交了。

### 步骤 1：查看改了什么

```bash
cd C:\Users\Administrator\b2b-pm-html-prototype-skill
git status
```

你会看到类似：

```
Changes not staged for commit:
  modified:   codex 版本的skill-fused/src/workflow/generation-pipeline.ts

Untracked files:
  codex 版本的skill-fused/src/reviews/new-file.ts
```

- `modified` = 改了的文件
- `Untracked` = 新增的文件（Git 还不认识的）

### 步骤 2：把改动添加到暂存区

```bash
git add "codex 版本的skill-fused/src/workflow/generation-pipeline.ts"
git add "codex 版本的skill-fused/src/reviews/new-file.ts"
```

> ⚠️ **注意**：路径里有空格，所以要用英文双引号 `""` 包起来。

**偷懒方法**（把所有改动一次性添加）：

```bash
git add "codex 版本的skill-fused/"
```

### 步骤 3：提交

```bash
git commit -m "你的提交说明"
```

**提交说明怎么写？** 遵循这个格式：

```
<类型>: <简短描述>
```

常用的类型：

| 类型         | 什么时候用      | 例子                    |
| ---------- | ---------- | --------------------- |
| `feat`     | 新功能        | `feat: 新增用户导出功能`      |
| `fix`      | 修复 Bug     | `fix: 修复审查系统未触发的问题`   |
| `docs`     | 改文档        | `docs: 更新开发者指南`       |
| `refactor` | 重构代码（不改功能） | `refactor: 简化生成流水线逻辑` |
| `test`     | 加测试        | `test: 补充审查系统的单元测试`   |

**错误示范**：`git commit -m "改了一下"` → 别人看不懂你改了啥

**正确示范**：`git commit -m "feat: 在生成流水线中集成 6 角色审查系统"`

### 步骤 4：推送到 GitHub

```bash
git push origin main
```

如果是第一次推送，可能需要输入 GitHub 用户名和密码（或者 token）。

---

## 五、完整流程速查表

从改代码到推送到 GitHub，完整流程就这 5 步：

```
┌─────────────────────────────────────────────────────┐
│  1. 打开项目                                          │
│     cd C:\Users\Administrator\b2b-pm-html-prototype-skill │
├─────────────────────────────────────────────────────┤
│  2. 改代码                                            │
│     在 codex 版本的skill-fused/ 目录下修改文件          │
├─────────────────────────────────────────────────────┤
│  3. 验证                                              │
│     npx tsc --noEmit     （类型检查）                  │
│     npx vitest run       （跑测试）                    │
├─────────────────────────────────────────────────────┤
│  4. 提交                                              │
│     git add "codex 版本的skill-fused/"                │
│     git commit -m "feat: 你的改动说明"                 │
├─────────────────────────────────────────────────────┤
│  5. 推送                                              │
│     git push origin main                              │
└─────────────────────────────────────────────────────┘
```

---

## 六、常见问题

### Q1：我在 OneDrive 那个目录改了代码怎么办？

OneDrive 目录是旧的备份，改了也不会影响 skill。你需要：

1. 把你在 OneDrive 里改的内容，**重新**在 Git 仓库里改一遍
2. 以后不要再打开 OneDrive 那个目录

### Q2：我在 `C:\Users\Administrator\.claude\skills\pm-html-pdt-fused\` 里改了代码怎么办？

没关系！因为这是 junction，你改的就是 Git 仓库里的文件。但为了养成好习惯，建议统一在 Git 仓库目录下操作。

### Q3：`git push` 要输入密码，但我不知道密码？

你需要一个 GitHub Personal Access Token（个人访问令牌）：

1. 打开 https://github.com/settings/tokens
2. 点 "Generate new token"
3. 勾选 `repo` 权限
4. 生成后复制 token
5. 推送时，用户名填 GitHub 用户名，密码填 token（不是 GitHub 密码）

### Q4：我想改一半，明天继续怎么办？

先临时保存：

```bash
git stash
```

明天继续时恢复：

```bash
git stash pop
```

### Q5：改坏了想回到上一个版本？

```bash
# 查看提交历史
git log --oneline

# 回到某个版本（把 abc123 换成你想回到的版本号）
git reset --hard abc123
```

> ⚠️ 这会丢掉所有未提交的改动，谨慎使用。

### Q6：我想加一个全新的文件怎么弄？

1. 在 `codex 版本的skill-fused/` 下创建文件
2. 写好内容
3. `git add "codex 版本的skill-fused/你的新文件路径"`
4. `git commit -m "feat: 新增 xxx 文件"`
5. `git push origin main`

### Q7：skill 加载的还是旧代码怎么办？

在 Trae IDE 中，关闭对话，重新打开一个新的对话窗口。Skill 会在新对话开始时重新加载。

---

## 七、总结：一句话记住

> **在 Git 仓库改 → typecheck → 测试 → git add → git commit → git push**
> 
> 就这 6 步，没了。
