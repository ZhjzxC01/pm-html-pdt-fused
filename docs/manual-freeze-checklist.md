# 人工冻结检查清单

日期：2026-04-26

## 目标

在代码质量门和自动一致性检查通过后，做最后一轮人工可用性核验。重点不是再找所有细节问题，而是判断当前版本是否可以作为冻结候选进入后续真实使用。

## 必看文件

费用报销：

- `examples/expense-approval/output/index.html`
- `examples/expense-approval/output/prototype-review.html`

合同审批：

- `examples/contract-approval/output/index.html`
- `examples/contract-approval/output/prototype-review.html`

## 检查项

干净原型：

- 页面能正常打开，没有空白页。
- 首屏能看出这是 B 端管理台，而不是纯文档页面。
- 列表、详情、新建、审批、日志等核心页面都存在。
- 关键操作按钮清晰，例如提交审批、审批通过、驳回、退回修改、管理员导出。
- 空态、异常态、无权限态至少在列表或关键页面中有表达。
- 页面没有明显的 `undefined`、`null`、`[object Object]` 等生成痕迹。

评审原型：

- 右侧存在 `PRD 标注` 面板。
- 页面上存在 P/M/F/A 编号标注。
- 点击或查看标注时，能理解它对应的 PRD 说明。
- 费用报销能看到报销金额、提交审批、主管审批、管理员导出。
- 合同审批能看到 50 万金额阈值、销售主管/法务/财务路径、退回修改、驳回后复制、当前审批节点和风险提示。

## 冻结标准

可以冻结：

- 自动检查全部通过。
- 两个干净原型都能作为评审材料打开。
- 两个评审原型都能说明 PRD 标注关系。
- 没有 A 类阻断问题或 C 类一致性问题。

暂缓冻结：

- HTML 打不开或关键页面缺失。
- 标注页没有 PRD 面板或标注编号。
- PRD、测试用例、原型之间出现明显冲突。
- 合同审批关键规则退化为通用模板。

## 当前自动核验结果

已通过：

- 费用报销干净原型
- 费用报销评审原型
- 合同审批干净原型
- 合同审批评审原型

自动核验覆盖：

- HTML 可解析。
- 关键业务文本存在。
- 关键 `data-*` 追踪属性存在。
- 评审原型存在 PRD 标注面板。
- 未发现 `undefined`、`null`、`[object Object]` 等明显生成痕迹。
