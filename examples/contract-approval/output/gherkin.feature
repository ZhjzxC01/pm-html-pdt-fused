# language: zh-CN
功能: 费用报销审批

  场景: 提交合同审批成功
    假如 经办人已登录
    当 点击新建合同
    并且 填写必填字段
    并且 点击提交审批
    那么 合同进入待审批状态

  场景: 审批通过
    假如 合同处于待审批
    当 审批人点击通过
    那么 合同进入已生效状态

  场景: 审批驳回
    假如 合同处于待审批
    当 审批人填写原因
    并且 点击驳回
    那么 合同进入已驳回状态

  场景: 金额阈值审批路径
    假如 合同金额超过审批阈值
    当 提交审批
    并且 销售主管通过
    并且 法务通过
    并且 财务通过
    那么 合同按销售主管、法务、财务路径审批并进入已生效状态

  场景: 退回修改后重新提交
    假如 合同处于待审批
    当 审批人点击退回
    并且 经办人修改信息
    并且 经办人重新提交
    那么 合同重新进入审批流

  场景: 驳回后复制新合同
    假如 合同处于已驳回
    当 点击复制为新合同
    并且 补充修改后提交
    那么 生成新的合同草稿，原合同保持已驳回

  场景: 经办人撤回
    假如 合同处于待审批
    当 经办人点击撤回
    那么 合同进入已撤回状态

  场景: 管理员导出
    假如 管理员已登录
    当 筛选数据
    并且 点击导出
    那么 系统导出符合条件的合同

